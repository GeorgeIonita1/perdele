'use client'

function TestClientComponent() {
    console.log('I am the test client component')
    return (
        <div>I am the client component</div>
    );
}

export default TestClientComponent;
